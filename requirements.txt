# PostgreSQL + FastAPI/Vue3 runtime. Legacy UI/container/local DB stacks are retired.
requests>=2.31.0
pandas>=2.0.3,<3
numpy>=1.24.3
plotly>=5.15.0
yfinance>=0.2.18
akshare>=1.11.0
tushare>=1.3.0
baostock>=0.9.2        # 证券宝:免费全历史(1990至今)K线,datahub kline 长历史源/独立兜底(无token、无 httpx 冲突)
httpx>=0.27.1           # mcp 需要
eltdx==0.5.1            # Python 3.10+ 生产 TDX 主兜底；MIT 许可，已验证分钟/日线/快照
PySocks>=1.7.1,<2       # eltdx 可选 HTTP/SOCKS 代理；仅局部注入 TDX 连接类
tdx-python==0.1.6; python_version >= "3.12"  # 3.12+ 增强兜底；原生 SDK 隔离子进程
zzshare>=0.4.9,<1       # A股正式 API:Token 由仓库外环境注入，分钟线主源
PyJWT[crypto]>=2.8,<3   # 原生 OIDC ID Token 签名/issuer/audience/exp/iat 校验
PyYAML>=6.0             # Shadow Platform Agent/LLM registry
# ⚠️ easy-tdx / mootdx 仅保留旧协议兼容，默认关闭；需要时单独安装 easy-tdx>=1.20.7,<2。
#    主 TDX 已迁到 eltdx。mootdx 包元数据锁 httpx<0.26,与上面 mcp 需要的
#    httpx>=0.27 直接冲突 → 若写进主依赖,pip 新解析器会 ResolutionImpossible 整个装不上。
#    故移出主依赖。**不装也不影响**:data_source_manager 的 mootdx 取数链已 try/except 守卫,缺库自动跳过。
#    需要它时单独装(实测 httpx0.27 兼容,装完把 httpx 再升回去即可):
#        pip install "mootdx>=0.11.0" && pip install -U "httpx>=0.27.1"
# ⚠️ TA-Lib(C 库,难装)同样**不放主依赖**:缺它不影响——instock_strategies 与 strategy_composer 均有
#    pandas 等价 shim(instock_strategies/_talib_compat.py),实测信号与真 talib 一致。想用真 talib 再单独装。
openai>=1.12.0
python-dotenv>=1.0.0
pytz
ta>=0.10.2
reportlab>=4.0.0
peewee>=3.17.0
schedule>=1.2.0
pywencai>=0.7.0
# adata 已于 2026-06-27 阶段1重构移除（二道贩子整合库；龙虎榜/资金流走东财直连）。
psycopg2-binary>=2.9.5
jieba>=0.42.1
beautifulsoup4>=4.12.0
lxml>=4.9.0
mcp>=1.2.0

# WebUI(新前端方案)
fastapi>=0.110
uvicorn>=0.27
redis>=5.0

# Shadow SDK 从 shadow-platform 仓库构建 wheel 后安装到同一 venv；不在此写私有 Git URL/Token。
shadow-platform==0.7.1
