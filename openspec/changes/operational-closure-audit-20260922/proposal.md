# 补齐两周执行记录中的运行闭环

连续任务记录表明：识别旧计划不足以生成次日新计划；新成交时间仍依赖数据库会话时区；
问财本地核验与后验任务摘要仍存在错误成功的边界。

补齐收盘计划生成和定时读取，统一新成交的时间语义，收紧参考源证据核验与局部失败状态。
保留正式选股成员、权重、冻结证据、原始持仓风险触发和既有权限；不引入交易执行能力。
无新增数据库表，利用既有指标快照和成交 extra 存储。旧数据不重写，旧计划继续作为历史参考。
