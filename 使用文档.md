# Shadow Foliant 使用文档

Shadow Foliant 是仅面向 A 股的个人投研、持仓管理和任务编排系统。系统输出用于辅助判断，
不构成投资建议，也不连接券商自动下单。

## 运行边界

- Web：FastAPI + Vue3 SPA，浏览器规范入口使用 OIDC Authorization Code + PKCE。
- 身份：普通页面要求 `stock-users`；持仓、成交、环境和任务控制要求 `stock-admins`。
- Agent：本地 stdio MCP 可直接使用；远程 MCP/HTTP 使用独立 Agent Bearer，不复用浏览器 Cookie。
- 数据：运行时只使用 PostgreSQL；SQLite、Streamlit 和 Docker 启动方式已退役。
- 任务：Web/MCP 只提交持久化任务，`jobs_hub` 负责实际消费。
- 部署：生产参数、真实域名和凭据只存在于仓库外受限配置。

## 本地安装

项目支持 Python 3.10+。Windows 命令在 Git Bash 中执行：

```bash
python -m venv .venv
source .venv/Scripts/activate
python -m pip install -r requirements.txt
cp .env.example .env
bash scripts/migrate.sh
```

`.env.example` 是唯一配置字段参考。至少准备 PostgreSQL；需要 AI 分析时再配置 Shadow LLM
或仓库外的兼容 Provider。Token、Cookie、client secret、真实主机和金融数据不得提交。

## 启动

Web 开发服务：

```bash
python webui/run_dev.py
```

默认访问 `http://localhost:8601`。任务进程另开终端：

```bash
python -m jobs.jobs_hub --serve
```

本地 stdio MCP：

```bash
python mcp_server.py
```

## 主要使用路径

### 看市场与研究

- 晨报、市场、板块和新闻用于了解当天偏涨、偏跌或震荡。
- 股票研究汇总行情、技术、基本面、风险和交易计划。
- 正式选股来自本地 PIT 数据、基本面、60 日核心技术结构、120/250 日风险修正和本地策略赛道。
- 问财与妙想只作为独立参考，不能增加正式候选、改变分数或绕过数据质量门槛。
- “低价擒牛”保留为参考策略，价格门槛为 `< 20`，不取代正式主链。

### 看持仓与动作

用户可见动作统一为四种：`加仓`、`不动`、`减仓`、`卖出`。硬风险和真实持仓状态的优先级
高于普通信号，外部参考与 LLM 只能解释，不能覆盖正式风控结论。

- 持仓总览：市值、盈亏、集中度和组合体检。
- 加仓审核：触发跌幅后检查仓位、加仓次数和基本面，再给出统一动作。
- 清仓助手：按止损、止盈、破位和长期占仓给出减仓/卖出顺序。
- 交易计划：给出入场区间、止损、目标和盈亏比，但只有统一动作是最终行动合同。

### 录入成交

Web 与 Agent 均支持成交导入。服务端会校验管理员权限、幂等指纹、账户引用和持仓影响；
成交可关联选股 run、提名、策略和决策信号，供后验分析。不要在日志或通知正文中输出完整
成交明细、Cookie、Token 或 client secret。

### 定时任务

任务启停、手动触发和环境配置属于管理员能力。任务提交后返回运行 ID；使用状态接口查询，
不要让 Web 请求同步等待长任务。部署或重启前必须避开行情预取、回测和研究长任务。

## 数据源

- 日线：新浪、BaoStock、东财、TDX、腾讯等按能力路由，AkShare 仅末位兜底。
- 分钟：zzshare 主源，TDX/eltdx 与其他可用源兜底。
- 问财：只作选股参考；Cookie 为可选的仓库外配置。
- 北向净流入：因公开口径长期停止更新，已退出采集、评分、通知和新库表。
- RAG：默认关闭；只有显式设置 `RAG_ENABLED=true` 并启用摄取任务才参与补充证据。

## Agent 建议顺序

1. 先读 `agent_cockpit`，确认任务、数据源和快照状态。
2. 个股优先调用 `research_stock(code, depth, view)`，读取统一 `trade_plan`。
3. 综合选股读取 `latest_selection`；正式 TOP15/TOP5 与外部参考分开显示。
4. 重任务使用 `trigger_task`，随后查询 `task_run_status`。
5. 写操作先 `dry_run=true` 预演，再由有权限的调用方确认执行。

完整工具契约见 [AGENT_SKILL.md](AGENT_SKILL.md)。

## 健康与排错

- `/healthz`：公开、无状态、无依赖详情。
- `/readyz`：受保护，检查运行版本和必要依赖。
- 登录失败：检查 OIDC issuer、audience、回调地址、PKCE/nonce/state 和系统时间。
- 403：检查 `stock-admins` 组，不要通过隐藏前端按钮绕过服务端授权。
- 任务不执行：检查 `jobs_hub` 和持久化任务状态。
- 数据降级：以接口返回的 data quality 为准，不把 partial/degraded 当完整成功。

数据库变更统一执行 `bash scripts/migrate.sh`；新迁移放入 `scripts/migrations/`，不得依赖应用
首次访问时偷偷改生产结构。
