# 通知内容审计与修复（2026-09-24）

证据范围：用户提供的 2026-09-20 至 09-24 QQ 消息摘录，部分监控消息未选入。这里的重复例子是可复现现象，不代表全量发送率或漏送率。未读取通知凭据、未实发测试 QQ、未自动下单。

| 样本现象 | 根因与处理 | 修复后 QQ 示例 |
| --- | --- | --- |
| 9/24 荣盛发展、均胜电子等同一报价时点成对提醒 | 同轮止损、目标与动作升级可各生成事件；现在每票同轮只保留最高优先级事件。不同调用路径的相同告警按标题和完整事件正文生成日内幂等键，由统一加密存档先抢占投递；抑制次数与 `prior_accepted` 等原因留在渠道回执。报价、判断或理由改变会形成新事件。持续风险满 180 分钟可再提醒。 | 同一时点荣盛发展只发一条“触及止损”；报价由 ¥1.21 变 ¥1.19 的新事件仍发。 |
| “动作：卖出/减仓｜买入区”；“当前¥7.33｜卖出/止损¥7.06” | 买入区只用于买入触发；等待与回避标作计划观察区。¥7.06 明确标作计划止损参考，不能当委托或成交价。原始触发、保护后的本轮判断及保护原因均留在快照。 | “当前¥7.33｜本轮判断卖出(...)｜计划止损参考¥7.06”；提示盘口、复权与时点需另核，未发生交易。 |
| 09:02 深成指/创业板指 0.00 被解释为震荡且长行截断 | 盘前不读取未形成的盘中指数和板块方向；QQ 只给待确认清单、昨日收益及时间。 | “⏳ 盘前待确认；A股盘前数据未形成；开盘后核实核心指数、持仓报价与风险触发条件。” |
| “正式TOP15 与正式TOP15重合：无” | 混用了问财旧对照与独立量化对照。现在标明正式↔独立、正式↔问财及 PIT 数据截至日。问财失败写“无法比较”，不写零重合。五组失败在 QQ 合为一行，逐组状态留在消息原稿与正式产物。 | “独立TOP15↔正式TOP15重合 N 只；正式TOP15↔问财五组：问财不可用，无法比较。” |
| 周报混入电影/篮球，妙想正文无可核验链接，压力预案给期权/期货指令 | 周报仅展示与持仓直接匹配且有官方 URL 的新闻线索，不推断影响；妙想完整原稿入加密存档，QQ 只提示来源不可核验；压力报告只解释情景，含具体交易动作的模型输出不发布。 | “本期没有与持仓直接相关且可核验的官方链接；不作影响判断。” |
| “真实胜率”“错过机会”“3次100%”“进化跑赢53.18%” | 周报把未了结浮动与已了结混合，当前未持仓不等于历史没买；展示口径改为“样本盈利占比（含未了结浮动）”，每票最近一次样本并去重。小样本标记仅描述；A/B 只称历史模拟差异，提示时间切分及同口径未核验。策略权重、模型反馈计算未改。 | QQ 长周报仅发有界索引，完整正文保存在加密消息存档及邮件渠道。 |

## 统计后续工作

独立统计任务应冻结推荐时点、入场与成熟窗口，分别报告已了结和持有中，按股票与事件两种分母去重；“历史未持仓”需逐日账户仓位证据。回测应输出触发数、成熟数、置信区间、训练/验证时间切分，以及进化组与默认组相同费用、风险预算、股票池和持有窗口的配对比较。在这些口径完成前，旧数字只作为带限制的历史样本描述。

## 首次上线观察与补充修复

`cf3364a` 于 14:26 上线，14:30 自然运行出现 23 条不同股票的即时提醒。该结果虽非同票成对重复，仍超出手机端的有界阅读目标。补充修复将每轮最多 6 条最高优先级事件逐条发出，剩余事件合为一条带股票代码与分类数量的提醒；完整事件仍保存在盘中快照。新版持续风险状态从当日已有快照继承，发布本身不会把原有风险视为新触发。此补充修复须经下一正常时点验证实际抑制与聚合效果。

`56497b6` 于 14:37 上线，14:57 自然轮询发出 6 条逐只提醒及 1 条汇总，证实上限生效；快照仍保留 11 个完整事件。进一步审计发现其中 7 个“动作升级”包含固定节点已记录原始卖出请求的股票：固定节点的保护动作与轻轮询原始请求跨路径比较，造成假升级。后续修复比较上一轮原始请求与本轮动作，保留真正新发生的止损和更强请求；该修复需下一正常交易时点被动验收。

## 验证与运维边界

相关单测覆盖同轮事件合并、并发/跨路径幂等、变化保留、冷却、价格标签、盘前零值、来源过滤、周报降级及 QQ 长文摘要。全量测试在 Python 3.12 隔离环境运行。上线后只检查受保护健康接口、存档抑制原因和下一正常计划时点；不触发测试推送。若运行健康或下一正常消息出现回归，按 `scripts/deploy.sh` 既有发布目录与当前软链接的回滚保护恢复上一版本，再核对受保护健康接口。消息已经投递的外部展示不能由存档回执反推。
