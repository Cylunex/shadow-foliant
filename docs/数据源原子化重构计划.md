# 数据层「源原子化」重构计划

> 状态:**阶段 0 + 阶段 1 已完成(2026-06-27,commit「数据源原子化重构 阶段0+阶段1」)**。
> 下次从 [§7 阶段 2](#7-分阶段实施计划)「补缺失直连真源 / 把现有直连归位到 `sources/*.py`」起手。
> 范围:`data/` 下数据层。门面 `datahub` 不变,只重构其下的「源」。

---

## 1. 背景与目标

### 1.1 一句话
把数据层从「**门面 datahub + 3 层抽象(adapter/manager/fetcher)+ 源散落 + 整合库黑盒**」重构成
「**门面 datahub(不变)+ 路由/降级(不变)+ 一家真源一个原子模块(直连、归一、无整合库)**」。

### 1.2 现在乱在哪(3 个病灶)
1. **同一家源散在 3-4 个文件**。例:**东财** 出现在 `a_stock_data_adapter`(push2/push2his/datacenter)+ `datahub` 内联(`_kline_eastmoney`/`_fund_nav_eastmoney`)+ `data_source_manager`(curl 东财)。想改"怎么调东财"要动 3 处。新浪、akshare 同样散落。
2. **路由套路由(嵌套降级)**。`datahub.kline(raw)` 的 `_route` 挂 `[fetcher, baostock, east, mootdx]`,而其中 `fetcher`(StockDataFetcher)**自己又是一条 8 源降级链**(→ `data_source_manager.get_stock_hist_data` → 新浪/东财/akshare/tushare/pywencai/yfinance/Ashare/mootdx)。结果 east/mootdx **在内外两层重复出现**、链路 4-5 跳。
3. **三层抽象边界模糊**:`adapter`(直连)← `manager`(包 adapter + 库源降级)← `fetcher`(包 manager + 技术指标)← `datahub`(全包 + 自己还内联)。谁包谁说不清。
4. **整合库黑盒**:`adata`/`akshare`/`tushare` 是第三方「整合体」,内部又偷偷包东财/新浪/同花顺等真源,**你不知道也控制不了它实际打哪**;它的源一断你受制于人。

### 1.3 目标收益
- 加新源 = 新建 1 个文件 + 在对应域的 `_route` 列表里登记 1 行。
- 改一家源 = 只动 1 个文件。
- 链路扁平(门面 → 路由 → 直连源,无嵌套降级)。
- 去掉整合库黑盒,直连真源,字段/单位/复权口径完全自控。

---

## 2. 设计原则(铁律)

1. **datahub 不变**:它仍是全项目唯一数据门面;`_route` 健康度路由 / 全源熔断 / 硬超时 / 三级缓存全保留。**降级只在 `_route` 这一层**。
2. **原子源模块**:一家真 provider 一个文件,**直连该家 API(HTTP/协议)、归一格式、内部不 import 任何整合库、自身不做跨源降级**。
3. **整合库处置(已拍板)**:
   - `adata` → **删**(二道贩子,真源都有直连)。
   - `akshare` → **降为各域末位兜底**(直连全挂时的安全网),绝不进主路径。
   - `tushare` → **保留为可选源**,但 **无 token 时 `available()=False`、根本不尝试**(不再像现在无 key 还走降级链空转)。
4. **格式契约**:所有源返回统一 schema(见 [§5.2](#52-归一契约)),下游无差别。重写直连必须**逐字段对齐**原有口径(volume 单位「股」、复权口径 raw/qfq、K线 `DatetimeIndex='Date'` + 大写 `Open/Close/High/Low/Volume`)。

---

## 3. 现状全景(勘探结论)

### 3.1 文件规模
| 文件 | 行数 | 现职责 | 重构后 |
|---|---|---|---|
| `data/datahub.py` | 1785 | 域函数(~40)+ `_route` + 三级缓存 + **部分源内联** | 门面+路由+缓存,**源全搬走** |
| `data/a_stock_data_adapter.py` | 1456 | 9 个直连 HTTP 源 + ~30 方法 | 拆进 `sources/*.py`,自身变薄 shim 或删 |
| `data/data_source_manager.py` | 1219 | 库源编排(akshare/tushare/pywencai/yfinance/Ashare/mootdx/adata)+ 转 adapter | 降级链摊平进 `_route`;库源拆进 sources |
| `data/stock_data.py` | 1150 | StockDataFetcher(新浪 K线主源 + 技术指标)→ 转 manager | 新浪→`sources/sina.py`;指标→独立 util |
| `data/baostock_safe.py` | 108 | 证券宝 K线(已原子) | → `sources/baostock.py`(改名归位) |
| `data/tdx_mootdx.py` | 180 | 通达信(已原子) | → `sources/mootdx.py` |
| `data/pywencai_safe.py` | 77 | 问财(已原子,带熔断) | → `sources/pywencai.py` |
| `data/akshare_safe.py` | 46 | akshare 超时/异常薄封装 | → `sources/_common.py` 的 `ak_safe` |
| `data/ashare_fallback.py` | 113 | 其实是直连新浪/腾讯 JSON | 归并进 sina/tencent |
| `data/northbound_cache.py` | 220 | 北向本地缓存 | 保留(缓存非源) |

### 3.2 `_route` 机制(保留不动)
`_route(domain, [(源名, 无参thunk), ...], empty, timeout)`:
- **健康度排序**:成功率为主(`ok/(ok+fail)`)− 延迟轻罚(最多 −0.2);连续失败 ≥2 且 120s 内 → 额外 −1.0;**未知源中立 0.5**(新源不抢主位)。
- **全源熔断**:某域所有源都在活跃冷却(score<−0.5)→ 直接返 empty,秒级降级,冷却 120s 自愈。
- **硬超时**:每源独立线程 + `future.result(timeout)`,默认 20s(kline 45s),超时 cancel 孤儿线程切下一源。
- **线程池** `_ROUTE_POOL`(64 线程):防批量假超时。
- **三级缓存**:L0 进程内存 → L1 Redis(JSON 友好) → L2 本地 pickle(含 DataFrame)。**缓存在域级**(miss 才走 `_route`);K线另有专用磁盘缓存(raw/qfq 两套 + 3 天 TTL + 失败回退历史)。

### 3.3 嵌套降级实例(要摊平的)
```
datahub.kline(code, '1y', adjust='raw')
  └─ _route("kline", [fetcher, baostock, east, mootdx])
       └─ fetcher = StockDataFetcher.get_stock_data
            └─ data_source_manager.get_stock_hist_data
                 └─ 降级链:新浪 → curl东财 → akshare → tushare → pywencai → yfinance → Ashare → mootdx
```
→ east/mootdx 在内层链 + 外层 `_route` **各出现一次**(重复);链路 4-5 跳。**目标**:把内层 8 源拆成独立原子源,全部摊进外层 `_route` 一层。

---

## 4. 外部真源清单(原子化目标 = 一家一模块)

| 真源 | 协议 | 现在散落处 | 目标模块 | 提供能力 |
|---|---|---|---|---|
| 东财 push2/push2his/datacenter/f10 | HTTP JSON | adapter + datahub 内联 + manager curl | `sources/eastmoney.py` | 行情·K线·龙虎榜·融资融券·解禁·大宗·股东户数·分红·基金净值·全球快讯·可转债比价·板块资金流 |
| 新浪 hq.sinajs/quotes.sina/财报 | HTTP | adapter + datahub(`_indices_sina`/`_kline_sina_qfq` 经 akshare)+ manager + fetcher | `sources/sina.py` | 行情·指数·K线(raw/qfq)·财报三表·行业 spot |
| 腾讯 qt.gtimg | HTTP | adapter `_tencent_quote` + datahub `_indices_tencent` | `sources/tencent.py` | 行情·指数 |
| 同花顺 10jqka | HTTP | adapter(一致预期/板块/热点) | `sources/ths.py` | 一致预期·板块排名/资金流·热点股 |
| 百度股市通 | HTTP | adapter | `sources/baidu.py` | 概念板块·K线 |
| 集思录 jisilu | HTTP | (现经 akshare `bond_cb_jsl`) | `sources/jsl.py` | 可转债 |
| 财联社 cls | HTTP | adapter `_cls_telegraph` | `sources/cls.py` | 快讯 |
| 巨潮 cninfo | HTTP | adapter `_cninfo_announcements` | `sources/cninfo.py` | 公告 |
| baostock 证券宝 | Python库(直连) | `baostock_safe.py` | `sources/baostock.py` | K线全历史(1990至今, raw/qfq) |
| 通达信 mootdx | 二进制协议 | `tdx_mootdx.py` | `sources/mootdx.py` | K线(独立协议兜底) |
| 问财 pywencai | Python库(→同花顺) | `pywencai_safe.py` | `sources/pywencai.py` | 选股(已带熔断) |
| 妙想 miaoxiang | HTTP(东财SaaS) | `analysis/miaoxiang.py` | (保留原位或 `sources/miaoxiang.py`) | 选股/诊断/研报 |
| **tushare** | Python库+token | manager | `sources/tushare.py` | 可选源,**无 token 不调** |
| **akshare** | Python库(整合体) | datahub 内联 + adapter + manager | `sources/akshare.py` | **末位兜底层**,按域归类 |
| **adata** | Python库(整合体) | manager | **删除** | (北向/龙虎/资金流/融资 → 东财直连) |
| yfinance / Ashare | Python库 | manager / ashare_fallback | 归并(yfinance 仅美股;Ashare→新浪/腾讯) | 兜底 |

---

## 5. 原子源模块设计

### 5.1 目录结构
```
data/sources/
  _common.py        # 归一工具(见 §5.3)
  eastmoney.py  sina.py  tencent.py  ths.py  baidu.py  jsl.py  cls.py  cninfo.py
  baostock.py  mootdx.py  pywencai.py  tushare.py
  akshare.py        # 末位兜底层
data/datahub.py     # 门面:域函数 _route([sources.x.cap, ...]) + 缓存
```

### 5.2 归一契约(每模块按能力暴露,返这些格式)
- `quotes(codes) -> {code6: {price, change_pct, pe_ttm, pb, mktcap, name, ...}}`
- `kline(code, period, interval, adjust) -> DataFrame`(`DatetimeIndex='Date'` + 大写 `Open/Close/High/Low/Volume`,volume 单位股)
- `indices() -> [ {name, code, price, change_pct} ]`
- `financials(code, report_type) -> [dict]` · `valuation(code) -> dict` · `full_valuation(code) -> dict`
- `north_flow(days) -> [dict]` · `capital_flow(code, days) -> [dict]` · `dragon_tiger(date) -> [dict]` · `margin(code) -> [dict]`
- `sector_ranking/sector_fund_flow/sector_spot -> ...` · `news/announcements -> [dict]` · `convertible_bonds() -> [dict]` · `fund_nav(code) -> DataFrame`
> 模块只实现该 provider **真能提供**的能力(东财能力最多;腾讯只 quotes/indices)。

### 5.3 `_common.py` 归一工具(从现有散落处收口)
- `norm_code(code) -> '600519'`(剥前缀/补零取后 6 位)
- `em_secid(code) -> '1.600519'`(东财 secid,沪 1/深京 0,处理 900沪B/920北交/科创 688)
- `bs_code(code) -> 'sh.600519'`(baostock)、`sina_code`/`tencent_code` 各家前缀
- 列名/单位归一:`to_ohlcv(df)`(大写 OCHLV + DatetimeIndex 'Date' + volume→股)
- `throttle(source)`(复用 `rate_limiter`)、`ak_safe(fn, *a)`(复用 `akshare_safe`,**仅 akshare 兜底层用**)
- `http_get_json(url, headers, timeout)`(统一 UA/超时/重试,直连源共用)

### 5.4 模块契约(每个 `sources/*.py` 遵守)
- 只直连本家 provider;**禁止 `import akshare/adata/tushare`**(akshare.py/tushare.py 除外)。
- 自身不做跨源降级、不读缓存(那是 datahub 的事)。
- 任何异常吞掉返空(空 DataFrame/`{}`/`[]`),绝不抛(让 `_route` 切下一源)。
- `if __name__ == '__main__':` 自测一两个样例,直连可达性自检。

---

## 6. 拆解地图(整合库调用 → 真源 → 处理)

| 现在的整合库调用(file) | 真源 | 处理 |
|---|---|---|
| `ak.stock_zh_a_hist`(datahub `_kline_akshare_qfq`) | 东财 push2his qfq | **冗余删**,用已有 `_kline_eastmoney(qfq)` |
| `ak.stock_zh_a_daily`(datahub `_kline_sina_qfq`) | 新浪 | 改**直连新浪** qfq |
| `ak.stock_individual_fund_flow`(datahub `_capital_flow_akshare`) | 东财 push2his | 并到东财资金流直连;akshare 末位 |
| `ak.stock_hsgt_fund_flow_summary_em`(datahub `_north_flow_akshare`) | 东财(已失效) | 换直连(同花顺/东财);akshare 末位 |
| `ak.stock_sector_spot(新浪行业)`(datahub `_sector_spot_sina`) | 新浪 | 直连新浪行业 spot |
| `ak.stock_board_industry_summary_ths`(datahub `_sector_spot_ths`) | 同花顺 | 并到同花顺直连 |
| `ak.stock_news_em`/`stock_info_global_em`(datahub) | 东财 | 换东财直连 |
| `ak.bond_cov_comparison`(datahub `_cb_eastmoney`) | 东财 | 直连东财可转债比价 |
| `ak.bond_cb_jsl`(datahub `_cb_jsl`) | 集思录 | 直连集思录 |
| `ak.fund_open_fund_info_em`/`fund_etf_hist_em`(datahub/manager) | 东财 f10 | 并到 `_fund_nav_eastmoney` |
| `ak.stock_financial_report_sina`(manager) | 新浪 | 并到新浪财报直连 |
| `adata` 实际只占 3 处(已核实) | — | **删 adata**,逐处定夺(下行) |
| ↳ `north_flow`(dsm 内 adata 兜底) | 同花顺(主源是**本地缓存**,jobs 15:40 入库) | 北向主源是同花顺缓存,**adata 只是 dsm 内兜底**→去掉无碍,**别误删缓存主源** |
| ↳ `dragon_tiger`(全市场明细 `dsm.get_dragon_tiger_detail_a_data`) | 东财 datacenter | 换 adapter `_dragon_tiger_board` 东财直连(个股 `dragon_tiger_stock` 本就东财直连) |
| ↳ `capital_flow_adata`(adata 独占域,补充源) | adata 独有字段 | adata 唯一独占域:**删此域** 或 换东财/百度直连(字段不同构,合并会污染口径) |
| `capital_flow`(主) | 东财 push2his(**已直连主源**) | 只需把 akshare 弱兜底(同走东财、非真跨源)降末位/删 |
| `margin/block_trade/holder/dividend/lockup` | 东财 datacenter | 已 adapter 直连,**不涉 adata**,阶段 3 归位即可 |
| `Ashare`(ashare_fallback) | 新浪/腾讯(本就直连) | 归并 sina/tencent |
| `tushare`(manager) | tushare 平台 | `sources/tushare.py`,无 token 不调 |

> 关键:**一大半 akshare/adata 调用其实是东财/新浪**,且我们 adapter 里**已有直连**——拆 = 换直连 + 删冗余,工作量比看上去小。

---

## 7. 分阶段实施计划

> 原则:**一阶段一 PR、一域一验证、datahub API 全程不变**。每阶段后 smoke 对照域输出逐字段一致。

### 阶段 0 — 脚手架(零行为变化)✅ 完成 2026-06-27
- [x] 建 `data/sources/` + `_common.py`(归一工具 `norm_code/em_secid/sina_code/tencent_code/bs_code/to_ohlcv/http_get_json/throttle/ak_safe`,从现有散落处收口;`python -m data.sources._common` 自测通过)。
- [x] 写「模块契约」到 `sources/README.md`。
- [x] **扫残留**:已 grep。应用层取数 2026-06-26 已收口 datahub;残留直 import adapter/manager 仅 `data/` 内部源 + `market_sentiment_data`(走 tushare,datahub 无对应方法,留)+ smoke_test(合法)。阶段 3 归位时再处理 `data/` 内部直 import。

### 阶段 1 — 快赢:主路径清整合库(纯冗余,低风险)✅ 完成 2026-06-27
- [x] K线 qfq:删 `_kline_akshare_qfq`(东财二道),`_route` 源链 5→4(`[sina_qfq, baostock_qfq, east_qfq, tickflow_qfq]`),全冷却短路判断同步去 `akshare_qfq` 健康键。
- [x] `capital_flow`:主源**已是**东财直连,akshare 弱兜底已在末位(prior hardening,无需再动)。
- [x] 基金净值 `fund_nav_history`:主用 `_fund_nav_eastmoney`,akshare 已末位(无需再动)。
- [x] **删 adata(已全删)**:
  - `north_flow`:去掉 dsm 内 adata 兜底,**保留同花顺本地缓存主源**。
  - `dragon_tiger`:adata `list_a_list_daily` → **东财数据中心直连**(`_dragon_tiger_eastmoney`,RPT_DAILYBILLBOARD_DETAILSNEW;归一保留 `stock_code/stock_name` 供 `wf_daily_strategy_scan`;无日期自动取最近有龙虎榜的交易日;live 验证 112 条)。
  - `capital_flow_adata`:退化为 canonical `capital_flow`(东财)别名,4 个调用方零改签名,数据从 adata 占位升级为东财真值。
  - `data_source_manager` 去掉 adata 初始化 + `get_dragon_tiger_detail_a_data`/`get_capital_flow_a_data`/`get_securities_margin_a_data`(末者本就无调用方);`northbound_cache.backfill_from_adata` 空壳化;`requirements.txt` 移除 adata。
- [x] 验证:py_compile 全过、离线归一/路由/别名 smoke 全过、消费方解析通过、live 龙虎榜直连 OK、清测试 mock 缓存污染。datahub 域函数签名/返回格式全程不变。

### 阶段 2 — 补缺失的直连真源
- [ ] `sources/sina.py`:新浪 qfq 日线(替 `ak.stock_zh_a_daily`)、行业 spot(替 `ak.stock_sector_spot`)、财报三表(归并 adapter)。
- [ ] `sources/eastmoney.py`:全球快讯(替 `stock_info_global_em`)、可转债比价(替 `bond_cov_comparison`)、基金/ETF 净值(替 `fund_*_em`)。
- [ ] `sources/jsl.py`:集思录可转债(替 `bond_cb_jsl`)。
- [ ] 每补一个 → `_route` 主源换成它 → akshare 同接口降末位。

### 阶段 3 — 现有直连归位 + 拆嵌套降级
- [ ] adapter 的东财/新浪/腾讯/同花顺/百度/财联社/巨潮方法 → 按 provider 拆进 `sources/*.py`;adapter 变薄 shim(保留转发以防残留 import)或删。
- [ ] datahub 内联源(`_kline_eastmoney`/`_indices_sina/_indices_tencent`/`_kline_baostock`/`_fund_nav_eastmoney`/`_sector_spot_*`/`_news_*`)→ 搬进对应 `sources/*.py`,datahub 只留 `_route` 声明。
- [ ] **拆 StockDataFetcher**:K线取数(新浪)→ `sources/sina.py`;`calculate_technical_indicators` → `data/indicators.py`(计算 util,非源)。
- [ ] **摊平嵌套降级**:`manager.get_stock_hist_data` 的 8 源链拆成独立原子源,统一进 `datahub.kline` 的 `_route` 一层(消除 fetcher 内的重复路由)。
- [ ] `sources/tushare.py`:`available()` 检 token,无 token → False,降级链跳过。

### 阶段 4 — 收尾
- [ ] `sources/akshare.py`:akshare 各接口统一这里,各域 `_route` 末位兜底。
- [ ] `requirements.txt`:移除 adata;保留 akshare(兜底)、tushare(可选)。
- [ ] `scripts/smoke_test_datahub_sources.py` 扩成「逐原子源直连可达 + 字段同构」校验。
- [ ] 更新 `ARCHITECTURE.md` / `交接说明.md`。

---

## 8. 验收 / 测试
- **回归**:每阶段后,`datahub` 各域函数输出与改前**逐字段一致**(用 smoke 脚本存基线对照)。
- **原子源自测**:每模块 `python -m data.sources.<x>` 独立可跑、直连可达。
- **字段同构**:重写直连必须对齐列名/单位/复权(volume 股、`Date` 索引 + 大写 OCHLV);`smoke_test_datahub_sources.py` 逐字段核。
- **健康度/降级回归**:断网/单源封禁场景下,`_route` 仍正确降级 + 缓存兜底(不退化)。

---

## 9. 风险与注意
- **残留直 import**:阶段 0 必须先扫清单(底层模块可能绕过 datahub 直 import adapter/manager)。
- **直连重写口径漂移**:akshare 帮你处理过的字段/单位/复权,直连要自己对齐——靠 smoke 逐字段兜。
- **摊平降级丢源**:把内层 8 源链摊进 `_route` 时,别漏掉只在内层出现的独有源(Ashare/yfinance/mootdx)。
- **工作量**:~5600 行跨 4 文件 + 多阶段,**别一把梭**;一阶段一 PR、一域一验证。
- **妙想**:`analysis/miaoxiang.py` 已是直连东财 SaaS 的原子源,是否搬进 `sources/` 看收益(它也被 MCP/选股直接用,搬动面大),可留原位。

---

## 10. 附录:域 → 源 现状映射(datahub.py 行号)

| 域函数 | 行号 | 现源链 | 源码位置 |
|---|---|---|---|
| quotes | 456 | a_stock → eastmoney → sina | adapter |
| kline(raw) | 871 | fetcher → baostock → east → mootdx | fetcher(嵌套8源)+ datahub内联 + 模块 |
| kline(qfq) | 871 | sina_qfq → baostock_qfq → east_qfq → akshare_qfq → tickflow_qfq | datahub内联(sina_qfq/akshare_qfq 经 akshare) |
| indices | 1153 | sina → tencent | datahub 内联 |
| north_flow | 1193 | dsm(adata/缓存) → akshare | manager + datahub akshare |
| capital_flow | 1234 | a_stock(东财) → akshare | adapter + datahub akshare |
| capital_flow_adata | 1250 | adata | manager(**删**) |
| dragon_tiger | 1258 | dsm(adata) | manager(**改东财直连**) |
| dragon_tiger_stock/detail | 1264/1270 | a_stock(东财 datacenter) | adapter |
| margin/block_trade/holder/dividend/lockup | 1286+ | a_stock(东财 datacenter) | adapter |
| sector_ranking | 1320 | a_stock(东财) → ths | adapter |
| sector_spot | 1365 | sina → ths | datahub 内联(均经 akshare) |
| sector_fund_flow | 1376 | push2 → bkzj → ths | adapter |
| financials/valuation/full_valuation | 1397+ | a_stock(新浪财报/腾讯+同花顺) | adapter |
| stock_reports/industry_reports/eps_forecast | 1417+ | a_stock(东财/同花顺) | adapter |
| stock_news | 1456 | dsm → akshare(东财) | manager + datahub akshare |
| market_news | 1486 | cls → em | dsm(财联社)+ datahub akshare |
| announcements | 1494 | dsm(巨潮) | manager → adapter |
| screen | 1504 | selection.data_source_config | 选股模块(auto/push2/pywencai/dataapi) |
| convertible_bonds | 1585 | eastmoney → jsl | datahub akshare(均经 akshare) |
| fund_nav_history | 1678 | eastmoney → akshare | datahub(东财直连 + akshare) |

---

## 11. 迁移范式示例(阶段 1 第一刀,可照搬)

以「删 K线 qfq 的 akshare 二道源」为例,演示**换源不动 datahub API**的标准做法:

**改前**(`datahub.py` qfq 链含 akshare 二道东财):
```python
_srcs_q = [_sina_q, _bao_q, _east_q, _aksh_q, _tick_q]   # _aksh_q = ak.stock_zh_a_hist(其实也是东财)
df = _route("kline_qfq", _srcs_q, ...)
```
**改后**(去掉 `_aksh_q`,东财已有 `_east_q` 直连;akshare 若留则降到末位):
```python
_srcs_q = [_sina_q, _bao_q, _east_q, _tick_q]            # 删冗余的 akshare 东财二道
# 若保留 akshare 兜底:_srcs_q = [_sina_q, _bao_q, _east_q, _tick_q, _aksh_q]  # 末位
df = _route("kline_qfq", _srcs_q, ...)
```
**验证**(范式,每刀都跑):
```python
# 1) 该域回归:改前后输出逐字段一致
import datahub
before = datahub.kline('600519','1y',adjust='qfq', use_cache=False)   # 改前先存基线
after  = datahub.kline('600519','1y',adjust='qfq', use_cache=False)   # 改后对照 len/列/末行
# 2) 健康度路由仍正常:多只票、断源场景 _route 正确降级
```
**要点**:datahub 域函数签名、返回格式、调用方**全程零改动**——只动 `_route` 的源列表。这就是"换源不动门面"的范式,阶段 1-2 每一刀都照此办。

---

## 12. 决策记录(已拍板,勿再反复)

| 项 | 决策 | 出处 |
|---|---|---|
| datahub 门面 | **不变**(路由/降级/缓存全保留) | 用户 2026-06-27 |
| `adata` | **删**(二道贩子,真源都有直连) | 用户 |
| `akshare` | **保留为各域末位兜底**,绝不进主路径 | 用户 |
| `tushare` | **保留为可选源**,但**无 token 时 `available()=False`、根本不尝试** | 用户 |
| 原子源模块 | 一家真源一文件、直连、归一、**禁 import 整合库**、不做跨源降级 | 设计 |
| 技术指标计算 | 不是源 → 阶段 3 从 StockDataFetcher 拆到独立 `data/indicators.py` | 设计 |
| 妙想 miaoxiang | 已是直连原子源,被 MCP/选股直接用,**可留原位**不强搬 | 设计 |

---
*建档 2026-06-27。下次开干从「阶段 1 快赢」起;先扫「残留直 import adapter/manager」清单(阶段 0)。*
